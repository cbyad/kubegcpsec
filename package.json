{
  "name": "kubegcpsec",
  "version": "0.0.1",
  "description": "A tool to generate k8s secrets manifests",
  "main": "./dist/index.js",
  "bin": {
    "kubegcpsec": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "tsc && node dist/index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cbyad/kubegcpsec.git"
  },
  "keywords": [
    "secret"
  ],
  "author": "yannick (cbyad)",
  "license": "ISC",
  "dependencies": {
    "commander": "^8.1.0",
    "yaml": "^1.10.2"
  },
  "devDependencies": {
    "@types/node": "^16.4.13",
    "typescript": "^4.0.3"
  }
}
